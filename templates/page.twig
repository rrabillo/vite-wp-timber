{% extends "base.twig" %}

{% block content %}
    <div class="wrapper-message-page py-5">
        <div class="container pt-5">
            <h1 class="text-center mt-5 mb-5">{{ post.title }}</h1>
            <div class="rte text-start">{{ post.content }}</div>
        </div>
    </div>
{% endblock %}